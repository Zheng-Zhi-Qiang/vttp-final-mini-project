export const delay = (ms: number) => new Promise(res => setTimeout(res, ms));

export const transition = (current_coord: google.maps.LatLngLiteral, target_coord_: google.maps.LatLngLiteral) => {
    
}